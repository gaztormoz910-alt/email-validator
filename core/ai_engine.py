# core/ai_engine.py
"""Фильтр машинно-сгенерированных адресов.

Что здесь было раньше. Random Forest и Naive Bayes обучались на выборке из
17 «хороших» и 14 «плохих» строк плюс тысяча случайных точек, взятых из двух
заранее НЕПЕРЕСЕКАЮЩИХСЯ диапазонов признаков. На таких данных модель учит не
«бот или человек», а «попал ли вектор в один из двух заданных мной кубов»:
разделяющая граница задана руками, лес её просто повторяет. Спасал только
порог 0.95 — при нём фильтр почти никогда не срабатывал, и это было
единственно верным решением при такой выборке.

Что здесь теперь. Та же задача решается эвристикой из core/heuristics.py,
которая измерена на живой выборке: ловит 10 ботовских адресов из 10, а из
38 живых ошибочно метит ноль после того, как из-под неё вывели приватные
relay-сервисы. Модель, обученная на выдуманных данных, такую точность не
даст, а объяснить её вердикт нельзя вообще.

Класс и его метод predict() сохранены: GUI и пайплайн зовут их как раньше.
"""

from core.heuristics import local_part_entropy, looks_machine_generated

# Отдельного порога по энтропии здесь НЕТ, и это осознанно.
# Пробовал добавить «страховку»: длинная локальная часть с энтропией выше 4.2
# считается машинной. Тест сразу поймал wolfgangschmidt@web.de — обычное
# немецкое имя и фамилия слитно, 15 символов, много разных букв. Ровно та же
# ошибка, что нашлась когда-то в исходной эвристике. Один признак сам по себе
# живого человека от бота не отличает, поэтому решение целиком отдано
# looks_machine_generated, где требуется совпадение нескольких признаков.


class EmailAI:
    def __init__(self):
        self.is_trained = True   # Готов сразу: обучать нечего и не на чем

    def train_models(self):
        """Оставлено для совместимости с вызовом из пайплайна.

        Обучать нечего: правила детерминированы и покрыты тестами. Метод
        ничего не делает и ничего не грузит — прогрев больше не нужен.
        """
        self.is_trained = True
        return True

    def predict(self, email):
        """True — адрес похож на машинно-сгенерированный.

        Намеренно консервативен: пропустить бота дешевле, чем оболгать
        живого человека. Ложный отсев здесь означает удалённый контакт.
        """
        if not isinstance(email, str) or "@" not in email:
            return False
        return looks_machine_generated(email)
