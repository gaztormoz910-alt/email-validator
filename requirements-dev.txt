# Зависимости для разработки и прогона тестов.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0

# Типы на ядре. Проверяется не весь проект, а список модулей из
# pyproject.toml — те, где ошибка типа означает неверный вердикт о живом
# человеке. Список расширяется, а не сокращается.
mypy>=1.11

# Статический разбор: неиспользуемые имена и обращения к несуществующим.
# Ловит то, чего не видит mypy на нетипизированных модулях.
pyflakes>=3.2
